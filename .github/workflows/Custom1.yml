name: Check
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      #---------------------------#
      - name: gui1
        run: 
          echo -------------------------
      - name: Set up NASM
        uses: ilammy/seup-nasm@v1.4.0
        with:
            version: 2.15.05
            One of: macosx
            platform: macosx
